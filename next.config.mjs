/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  // images: {
  //   domains: ["flagcdn.com"],
  // },
};

export default nextConfig;
